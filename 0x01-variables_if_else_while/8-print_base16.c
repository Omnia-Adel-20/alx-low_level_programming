#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: prints all the numbers in lowercase
 *
 * Return: always 0 (success)
*/

int main(void)
{
	int i;
	int m,

	for (i = 48; i <= 57; i++)
	{
		putchar(i);
	}
	for (m = 97; m <= 102; m++)
	{
		putchar(m);
	}
	putchar('\n');

	return (0);
}
