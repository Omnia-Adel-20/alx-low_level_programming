#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: prints all the numbers in lowercase
 *
 * Return: always 0 (success)
*/

int main(void)
{
	int i = 48;

	while (i <= 57 && i <= 102)
	{
		putchar(i);
		i++,
	}
	putchar('\n');

	return (0);
}
