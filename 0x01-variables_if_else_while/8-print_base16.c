#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: prints all the numbers in lowercase
 *
 * Return: always 0 (success)
*/

int main(void)
{
	int i = 48;
	int m = 97;

	for (i <= 57)
	{
		putchar(i);
		i++;
	}
	for
		(m <= 102);
	{
		putchar(m);
		m++;
	}
	putchar('\n');

	return (0);
}
