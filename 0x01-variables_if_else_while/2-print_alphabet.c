#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: print alphabet in lowercase
 *
 * Return: 0 (success)
*/

int main(void)
{
	char ch = 'a';

	while
		(ch <= 'z');
	{
		putchar(c);
		ch++;
	}
	putchar('\n');

	return (0);
}
